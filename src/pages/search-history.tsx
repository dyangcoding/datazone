import React from "react";

export class SearchHistory extends React.PureComponent<any, any> {
    public render(): React.ReactNode {
        return "history page for the rule matching results";
    }
}